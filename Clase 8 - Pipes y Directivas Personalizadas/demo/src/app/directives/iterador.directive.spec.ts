import { IteradorDirective } from './iterador.directive';

describe('IteradorDirective', () => {
  it('should create an instance', () => {
    const directive = new IteradorDirective();
    expect(directive).toBeTruthy();
  });
});
