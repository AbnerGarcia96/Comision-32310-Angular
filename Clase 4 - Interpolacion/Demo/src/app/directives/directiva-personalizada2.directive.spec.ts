import { DirectivaPersonalizada2Directive } from './directiva-personalizada2.directive';

describe('DirectivaPersonalizada2Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPersonalizada2Directive();
    expect(directive).toBeTruthy();
  });
});
