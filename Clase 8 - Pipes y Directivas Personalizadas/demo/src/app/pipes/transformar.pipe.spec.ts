import { TransformarPipe } from './transformar.pipe';

describe('TransformarPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarPipe();
    expect(pipe).toBeTruthy();
  });
});
